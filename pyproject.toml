[project]
name = "better-call-alex"
version = "0.1.0"
description = "Local US case-law research workbench — ETL package (stdlib only)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=2.5.2",
    "scipy>=1.18.1",
]

[tool.uv]
package = false
