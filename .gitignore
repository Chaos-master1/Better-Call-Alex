data/
.venv/
__pycache__/
*.pyc
node_modules/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.tsbuildinfo
.shards/
next/
app/.next/
.mimosa/
.zcode/

# Local environment (see .env.example for the variables the app reads)
.env
.env.*
!.env.example

# Historical plan archive (2.6 GB of superseded planning docs) — deliberately
# untracked; never commit it (2026-09-20 independent audit).
better call alex old plan archives/

# Independent audit scratch runs
corpus-audit-tmp/
